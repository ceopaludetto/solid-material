---
title: TextField
layout: ~/layouts/docs.astro
---

import Playground from "~/components/playground.astro";
import {
  BasicExample,
  AdornmentExample,
  DescriptionExample,
  ErrorExample,
  DisabledExample,
} from "~/components/examples/text-field";

# {frontmatter.title}

Text Fields let users enter and edit text.

## Basic usage

The `TextField` needs a label.

<Playground>
  <BasicExample client:load />
  <div slot="code">
    ```tsx
    <TextField label="E-mail" />
    ```
  </div>
</Playground>

## Adding adornments

You can add adornments by supplying `startAdornment` and `endAdornment` props:

<Playground>
  <AdornmentExample client:load />
  <div slot="code">
    ```tsx
    <TextField label="E-mail" startAdornment={() => <User />} />
    <TextField
      label="E-mail"
      endAdornment={() => (
        <IconButton variant="standard">
          <Eye />
        </IconButton>
      )}
    />
    ```
  </div>
</Playground>

## Description

You can add description to `TextField` using the `description` prop:

<Playground>
  <DescriptionExample client:load />
  <div slot="code">
    ```tsx
    <TextField label="E-mail" description="Some random description" />
    ```
  </div>
</Playground>

## States

### Error

You can set `validationState` prop to `invalid` to get a nice visual representation of an error:

<Playground>
  <ErrorExample client:load />
  <div slot="code">
    ```tsx
    <TextField label="E-mail" validationState="invalid" description="Some random error" />
    ```
  </div>
</Playground>

### Disabled

You can disable the `TextField` using the `isDisabled` prop:

<Playground>
  <DisabledExample client:load />
  <div slot="code">
    ```tsx
    <TextField label="E-mail" isDisabled />
    ```
  </div>
</Playground>
